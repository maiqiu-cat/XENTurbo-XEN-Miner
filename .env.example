# Optional custom RPC overrides (comma-separated allowed). Users can also set these in-app.
VITE_RPC_ETH=
VITE_RPC_POLYGON=

# Optional GA4 analytics. The tag loads only after consent and only on the allowed hostname.
VITE_GA_MEASUREMENT_ID=
VITE_GA_ALLOWED_HOSTNAME=
